import { StyleSheet } from "react-native"
import { green } from "./style-variables.js"

const styles = StyleSheet.create({
  dashContainer: {
    backgroundColor: green,
    width: "100%",
    height: "100%"
  }
})

export default styles